@model IEnumerable<SakuraWeb.Models.Korisnik>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.korisnickoIme)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.emailAdresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.lozinka)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.jePretplacenNaNewsletter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ulogaKorisnika)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.korisnickoIme)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.emailAdresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.lozinka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.jePretplacenNaNewsletter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ulogaKorisnika)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
